---
deployment:
  tasks:
    - export DEPLOYPATH=/home/vjra05pudskc/public_html/
    - npm run export
    - /bin/rsync -av --exclude=".git" --exclude=".next" --exclude="node_modules" ./out/ $DEPLOYPATH